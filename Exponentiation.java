public class Exponentiation{
	double exponentiationFunction(int modulus, int power){
		return Math.pow(modulus, power);
	}
	public static void main(String[] args) {
		int mod, power;
		Exponentiation expo = new Exponentiation();
		expo.exponentiationFunction(mod, power)
	}
}